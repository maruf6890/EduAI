/**
 * Shared classroom types.
 *
 * NOTE: reconcile this with the `ClassroomCard` type exported from
 * '@/components/mvpblocks/Cardflip' if the shapes diverge — this file
 * assumes a superset that covers both the card list and the detail page.
 */

// export interface ClassroomStats {
//     students: number;
//     assignments: number;
//     quizzes: number;
// }

// export interface ClassroomInstructor {
//     id?: string;
//     name: string;
//     avatarUrl?: string;
// }

// export interface Classroom {
//     id: string;
//     name: string;
//     code?: string;
//     semester?: string;
//     instructor?: ClassroomInstructor;
//     coverColor?: string;
//     stats: ClassroomStats;
// }